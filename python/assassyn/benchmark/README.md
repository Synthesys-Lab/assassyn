# Assassyn Benchmark Helpers

This package contains small benchmark harnesses for comparing Assassyn execution
paths. The helpers are intended for ROI-scoped measurements and CI smoke tests,
not for machine-independent pass/fail performance thresholds.
